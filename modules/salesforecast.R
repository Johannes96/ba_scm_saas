# UI ----------------------------------------------------------------------

salesforecast_UI <- function(id) {
  ns <- NS(id)
  tagList(
    
    
    
  )
}

## Server ------------------------------------------------------------------

salesforecast <- function(input, output, session) {





}
